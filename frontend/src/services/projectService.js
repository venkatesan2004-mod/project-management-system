import api from './api'; export const getProjects = (params) => api.get('/projects', { params }).then((r) => ({ ...r.data.data, meta: r.data.meta })); export const getProject = (id) => api.get(`/projects/${id}`).then((r) => r.data.data.project); export const createProject = (data) => api.post('/projects', data).then((r) => r.data.data.project); export const updateProject = (id, data) => api.put(`/projects/${id}`, data).then((r) => r.data.data.project); export const deleteProject = (id) => api.delete(`/projects/${id}`);
