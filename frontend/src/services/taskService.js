import api from './api'; export const getTasks = (params) => api.get('/tasks', { params }).then((r) => ({ ...r.data.data, meta: r.data.meta })); export const createTask = (data) => api.post('/tasks', data).then((r) => r.data.data.task); export const updateTask = (id, data) => api.put(`/tasks/${id}`, data).then((r) => r.data.data.task); export const completeTask = (id) => api.patch(`/tasks/${id}/complete`).then((r) => r.data.data.task); export const deleteTask = (id) => api.delete(`/tasks/${id}`);
